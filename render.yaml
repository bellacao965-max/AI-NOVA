services:
  - type: web
    name: nova-ai
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    rootDir: ./
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "5000"
